'use client';

// Rail is kept as an empty slot so ReadingGrid renders the left spine line.
// Progress moved to SessionHUD. TOC toggle is in SessionHUD's right side.
export default function SessionRail() {
  return null;
}
