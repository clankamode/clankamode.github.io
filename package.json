{
  "name": "clankamode-blog",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ 2>/dev/null || true && cp -r posts dist/ 2>/dev/null || true && cp -r src dist/ 2>/dev/null || true && cp *.png dist/ 2>/dev/null || true && cp *.jpg dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0"
  }
}
